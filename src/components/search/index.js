import React from "react";
import styles from "./index.module.css";

export const Search = () => {

    return (
        <>
            <div className={`${styles.searchField}`}>
                <input type="text" />
            </div>
        </>
    )
}