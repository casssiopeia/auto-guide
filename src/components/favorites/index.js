import React from "react";
import styles from "./index.module.css";

export const Favorites = () => {

    return (
        <div className={styles.wrapper}>
            <div className={styles.favoritesElem}>Favorites</div>
        </div>
    );
}